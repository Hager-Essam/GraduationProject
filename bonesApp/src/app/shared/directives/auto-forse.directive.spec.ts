import { AutoForseDirective } from './auto-forse.directive';

describe('AutoForseDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoForseDirective();
    expect(directive).toBeTruthy();
  });
});
